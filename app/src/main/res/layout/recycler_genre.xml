<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="genre"
            type="String" />

        <variable
            name="viewModel"
            type="com.podium.technicalchallenge.ui.genre.GenreViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/linearLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="4dp">
        <View
            android:id="@+id/start_line"
            android:layout_width="12dp"
            android:layout_height="1dp"
            android:layout_marginHorizontal="8dp"
            android:background="@color/divider_color"
            app:layout_constraintBottom_toBottomOf="@+id/title_text_view"
            app:layout_constraintEnd_toStartOf="@id/title_text_view"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@id/title_text_view" />

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/title_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="?android:attr/textAppearanceLarge"
            android:text="@{genre}"
            android:textColor="@color/divider_color"
            app:layout_constraintEnd_toStartOf="@+id/end_line"
            app:layout_constraintStart_toEndOf="@id/start_line"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Thriller" />

        <View
            android:id="@+id/end_line"
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_marginHorizontal="8dp"
            android:background="@color/divider_color"
            app:layout_constraintBottom_toBottomOf="@+id/title_text_view"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/title_text_view"
            app:layout_constraintTop_toTopOf="@id/title_text_view" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/movie_genre_recycler"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:itemBinding="@{viewModel.genreMovieBinding}"
            app:items="@{viewModel.getMoviesByGenre(genre, false)}"
            android:orientation="horizontal"
            app:layout_constraintTop_toBottomOf="@id/title_text_view"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            tools:listitem="@layout/recycler_movie" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>